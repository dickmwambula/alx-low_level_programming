#include <stdio.h>
/**
 * main - A program that will print a single number to base ten
 *
 * Return:0 (Successful)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
